rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 업로드한 학습지 이미지: 본인 폴더에만 읽기/쓰기
    match /worksheets/{uid}/{file=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
